__pycache__/
# Python / build
__pycache__/
*.pyc
.venv/
env/
.env

# Editor / OS files
.DS_Store
Thumbs.db

# Local build artifacts and backups
*.bak
*.jar
*.pack
*.idx
*.jar 

# Common build / artifacts folders
build/
downloads/

# Local generated outputs
output/
output/fhir/
analytics/test_output/

# Keep Synthea source files but ignore tests and local artifacts
# (do NOT ignore source CSVs or resources under synthea/*)
**/src/test/
**/test/
**/tests/
**/*Test.*

# Synthea: local artefacts / downloads / build outputs
synthea/downloads/
synthea/**/build/
synthea/**/libs/

# Keep synthea_original source but ignore its internal git objects and build artifacts
synthea_original/.git/objects/
synthea_original/.git/objects/pack/
synthea_original/build/
synthea_original/build/libs/

# Keep other synthea-* source trees; ignore their internal git objects if present
synthea-source/.git/objects/
synthea-source/.git/objects/pack/
synthea-international/.git/objects/
synthea-international/.git/objects/pack/
