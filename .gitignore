__pycache__/
*.pyc
.venv/
env/
.env

# Local build artifacts and backups
*.bak
# *.jar
*.pack
*.idx

# Common build / artifacts folders
build/
__pycache__/
*.pyc
.venv/
env/
.env

# Local build artifacts and backups
*.bak
*.idx

# Common build / artifacts folders
build/
downloads/

# Application/output directories (local/generated files)
output/
output/fhir/
analytics/test_output/

# Synthea: keep source, but ignore tests and local artifacts
# (we explicitly DO NOT ignore the `synthea/` source tree)
**/src/test/
**/test/
**/tests/
**/*Test.*

# Synthea-specific local artifacts
# synthea/downloads/
# synthea/**/build/
# synthea/**/libs/

# synthea_original: keep sources but ignore its internal git objects and build artifacts
# synthea_original/.git/objects/
# synthea_original/.git/objects/pack/
# synthea_original/build/
# synthea_original/build/libs/

# Editor / OS files
.DS_Store
Thumbs.db